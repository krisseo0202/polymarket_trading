# CEO Plan — 2026-04-03
> Framing: Ken Griffin lens. Evidence before parameters. Operational validation before scaling.

## Context

Infrastructure is clean:
- `bot.py` refactored to 347 lines (was 1,716)
- `CycleRunner`, `Services`, `init_services` extracted to proper modules
- Feed hardening complete: stale detection, reconnect, slot regression prevention
- 197 tests passing
- Branch: `refactor/simplify-strategies-and-models`

**Critical gap:** zero empirical evidence that any strategy has edge. All tomorrow's work is operational validation, not new building.

---

## Tomorrow's Steps (in priority order)

### 1. Run one cycle end-to-end in paper mode
```bash
python bot.py --strategy btc_updown_xgb --paper --no-confirm
```
Watch for:
- [ ] Market discovery succeeds
- [ ] Chainlink captures slot-open at the 5-min boundary (`"New slot XXXXX: Chainlink open price = $X"`)
- [ ] CycleRunner completes without error
- [ ] State file written after cycle

### 2. Verify price-tick log is writing
```bash
tail -5 logs/price_ticks.jsonl
```
- [ ] File exists with entries after one cycle

### 3. Verify slot-open capture timing
Watch logs around the :00/:05/:10 boundary. Confirm:
- [ ] `New slot` message fires on the *current* slot (not previous)
- [ ] Strike source = `"chainlink"` (not `"regex"` or `"unknown"`)

### 4. After 3–5 clean paper cycles: merge to master
```bash
git checkout master && git merge refactor/simplify-strategies-and-models
```
- [ ] All tests still pass on master after merge

---

## Hard stops (do not do tomorrow)

- Do NOT enable live trading
- Do NOT tune `min_edge`, `kelly_fraction`, or model thresholds
- Do NOT add new strategies or features
- Do NOT modify dashboard

---

## Next gate after validation

Once 3–5 clean paper cycles are confirmed:
1. Collect 1–2 days of paper trade data
2. Check calibration: are strategy `prob_yes` values actually predictive?
3. Only then consider threshold tuning or live ramp

The next gate is **operational validation**, not more infrastructure.
